import { createTheme } from '@mui/material'

const theme = createTheme({
    palette: {
        primary: {
            main: "#1b479e",
        },
        secondary: {
            main: "#f977cc"
        },
        pokemon: {
            main: "#f977cc"
        }
    }
})

export default theme